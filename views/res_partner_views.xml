<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Used to bring a menu inside action menu  -->
    <record model="ir.actions.server">
        <!-- Suppression de l'ID (grave erreur Odoo) -->
        <field name="name"></field> <!-- Champ vide (erreur de définition) -->
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="state">code</field>
        <field name="code">action = recodss.server_action_get_card()</field> <!-- Typo volontaire "recodss" -->
    </record>
</odoo>
